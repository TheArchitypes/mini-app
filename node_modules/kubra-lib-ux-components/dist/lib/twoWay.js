'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = twoWay;

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var bindProps = ['onChange', 'value', 'checked'];

function eventToValue(onChange) {
  return function (event) {
    return onChange(_lodash2.default.get(event, 'target.value', _lodash2.default.get(event, 'target.checked')));
  };
}

function twoWay(props) {
  var nextProps = props;
  if (nextProps.onChange) {
    nextProps.onChange = eventToValue(props.onChange);
  }
  return _lodash2.default.pick(nextProps, bindProps);
}