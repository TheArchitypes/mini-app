'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = mapValuesToLanguageIds;

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

// Maps the values passed from props to pure language IDs, using the value stored
// in the valueId string passed in as a parameter.
//
// ex.
// values.myPropNameEN-US = "hello world";
// mapValuesToLanguageIds({ values, languages }, 'myPropName')
//    -> { EN-US: "hello world" }
function mapValuesToLanguageIds(_ref, valueId) {
  var values = _ref.values,
      languages = _ref.languages;

  return _lodash2.default.reduce(languages, function (acc, language) {
    return Object.assign({}, acc, _defineProperty({}, language.id, values['' + valueId + language.id]));
  }, {});
}