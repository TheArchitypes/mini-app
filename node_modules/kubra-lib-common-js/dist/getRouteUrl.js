'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getRouteUrl;

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getRouteUrl(routeString) {
  var params = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

  // If no route string, return null
  if (!routeString) return null;
  var linkString = routeString;

  // Loop over params and replace
  _lodash2.default.each(params, function (value, key) {
    linkString = linkString.replace(':' + key, value);
  });

  return linkString;
}