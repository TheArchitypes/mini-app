'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = joinRequestParameters;

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function joinRequestParameters(requestParams) {
  if (!requestParams) return '';
  // Return the parameter if it's already a string.
  if (typeof requestParams === 'string') return requestParams;

  // Map the params to an array of key=val, and join them.
  var requestParameterString = _lodash2.default.map(requestParams, function (val, key) {
    return key + '=' + val;
  }).join('&');

  return '?' + requestParameterString;
}