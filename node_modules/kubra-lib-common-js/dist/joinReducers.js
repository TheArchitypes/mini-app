'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.joinReducers = joinReducers;

var _seamlessImmutable = require('seamless-immutable');

var _seamlessImmutable2 = _interopRequireDefault(_seamlessImmutable);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function joinReducers(subReducers) {
  return function (state, action) {
    var isInitializing = state === undefined;
    var initialState = _seamlessImmutable2.default.from({});
    return subReducers.reduce(function (nextState, reducer) {
      if (isInitializing) {
        return _seamlessImmutable2.default.merge(nextState, reducer(undefined, action));
      }
      return _seamlessImmutable2.default.merge(nextState, reducer(nextState, action));
    }, state || initialState);
  };
}

exports.default = {
  joinReducers: joinReducers
};