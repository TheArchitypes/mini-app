import Promise from 'bluebird';

export function branch(condition, truePromiseFunc, falsePromiseFunc = () => Promise.resolve()) {
  if (condition) {
    return truePromiseFunc();
  }
  return falsePromiseFunc();
}

export default branch;
